# Secrets - never commit
**/.env
automation/daily-briefing/config.yaml

# Access codes & API secrets — server-only, NEVER commit
server/portfolio-api/access-codes.json
**/access-codes.json
**/api.env
*.secret

# Python
__pycache__/
*.py[cod]
*.egg-info/
.venv/
venv/

# OS
.DS_Store
Thumbs.db

# Build artifact — generated by build.py, rebuilt in CI
dist/

# Deploy keys — never commit
*-deploy
*-deploy.pub
id_deploy*

# Logs
*.log

# User journals — keep private, decide later if want versioning
journals/

# IDE
.vscode/
.idea/
